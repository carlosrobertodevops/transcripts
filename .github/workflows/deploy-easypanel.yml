name: Deploy EasyPanel

on:
  workflow_call: {}

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: easypanel
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to EasyPanel
        run: |
          # TODO: Implement EasyPanel deployment
          # Prerequisites: EASYPANEL_API_TOKEN, EASYPANEL_PROJECT_ID in secrets
          # Steps:
          # 1. Trigger deployment via EasyPanel API
          # 2. Poll deployment status
          # 3. Verify health endpoint
          echo "Deploying to EasyPanel..."
          echo "Project ID: ${{ secrets.EASYPANEL_PROJECT_ID }}"
