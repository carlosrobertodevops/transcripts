import { LoginForm } from '@/components/auth/login-form'

export const metadata = {
  title: 'Entrar — transcripts',
  description: 'Faça login na sua conta transcripts',
}

export default function LoginPage() {
  return <LoginForm />
}
