import { RegisterForm } from '@/components/auth/register-form'

export const metadata = {
  title: 'Criar conta — transcripts',
  description: 'Crie sua conta transcripts gratuita',
}

export default function RegisterPage() {
  return <RegisterForm />
}
